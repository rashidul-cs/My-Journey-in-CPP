
#include <iostream>
using namespace std;
int main(){
    string s = "Raghav ";  //in string there is always a Null Character in the last[ '\0' ]
    cout << s.length(); // we can also use here [ s.size() ]
    //cout << endl;
    //char ch = '\0';
    //cout << (int)ch;
    //cout << endl;
    //int x = 0;
    //cout << (char) x;  //that means Null Character (ASCCI Value)
    return 0;
}
